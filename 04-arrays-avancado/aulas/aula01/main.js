const nomes = ['Karine', 'Maria', 'Andressa', 'Cookie', 'Doritos'];
const novo = nomes.slice(0, -1);
console.log(novo);

const frutas = 'Maçã Banana Uva';
const fruta = frutas.split(' ');
console.log(fruta);

const listas = ['PowerBank', 'Bolsa', 'Garrafa'];
const lista = listas.join(', ');
console.log(lista);